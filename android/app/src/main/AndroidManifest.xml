<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE"/>
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <!-- Voice input (talk-to-app). Marked optional so Fire TV / mic-less devices
         still install — the app gracefully degrades to text input on those. -->
    <uses-permission android:name="android.permission.RECORD_AUDIO" />
    <uses-permission android:name="android.permission.MODIFY_AUDIO_SETTINGS" />
    <uses-feature android:name="android.hardware.microphone" android:required="false" />

    <!-- Device alerts. POST_NOTIFICATIONS is a runtime prompt on Android 13+;
         Fire OS 7 is Android 9, so Fire TV grants it at install with no prompt.
         BOOT_COMPLETED restarts the five-minute alert poll, which is a chain of
         one-shot jobs and would otherwise stay dead after a reboot. -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.RECEIVE_BOOT_COMPLETED" />
    <!-- Required on Android 11+ (API 30) to enumerate every installed package -->
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES"
        tools:ignore="QueryAllPackagesPermission" />

    <!-- Billing: lets the app ask whether anything can open an https payment
         link (Chrome Custom Tab). Fire TV usually has nothing; the app then
         shows a QR code instead. Required on Android 11+ for resolveActivity. -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <category android:name="android.intent.category.BROWSABLE" />
            <data android:scheme="https" />
        </intent>
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true"
        android:largeHeap="true"
        android:networkSecurityConfig="@xml/network_security_config">

        <!-- Splash Activity - This is now the launcher -->
        <activity
            android:name="com.snowmedia.SplashActivity"
            android:exported="true"
            android:theme="@style/SplashTheme"
            android:label="@string/app_name"
            android:launchMode="singleTask"
            android:banner="@mipmap/ic_launcher">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>

        </activity>

        <!-- Main Activity - Now also handles SSO deep links -->
        <activity
            android:exported="true"
            android:launchMode="singleTask"
            android:alwaysRetainTaskState="true"
            android:label="@string/app_name"
            android:name="com.snowmedia.MainActivity"
            android:theme="@style/AppTheme.NoActionBar">

            <!-- Custom scheme deep link: snowmedia://sso?token=... -->
            <intent-filter android:label="Snow Media SSO">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="snowmedia" android:host="sso" />
            </intent-filter>

            <!-- Universal/App Link: https://snowmediaent.com/sso?token=... -->
            <!-- autoVerify requires assetlinks.json hosted at https://snowmediaent.com/.well-known/assetlinks.json -->
            <intent-filter android:label="Snow Media SSO Web" android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" />
                <data android:host="snowmediaent.com" />
                <data android:pathPrefix="/sso" />
            </intent-filter>

            <intent-filter android:label="Snow Media SSO Web (www)" android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https" />
                <data android:host="www.snowmediaent.com" />
                <data android:pathPrefix="/sso" />
            </intent-filter>
        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths" />
        </provider>

        <!-- Accessibility Service that auto-taps Storage → Clear cache in system Settings.
             User must enable it once in Android Settings → Accessibility → Snow Media Cache Cleaner. -->
        <service
            android:name="com.snowmedia.appmanager.CacheClearService"
            android:exported="true"
            android:label="Snow Media Cache Cleaner"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/cache_clear_accessibility_service" />
        </service>

        <!-- Re-arms the alert poll after a reboot. -->
        <receiver
            android:name="com.snowmedia.notify.BootReceiver"
            android:exported="true"
            android:enabled="true">
            <intent-filter>
                <action android:name="android.intent.action.BOOT_COMPLETED" />
                <action android:name="android.intent.action.QUICKBOOT_POWERON" />
            </intent-filter>
        </receiver>

        <!-- Fires when the viewer swipes an alert away, so the poll stops
             re-posting it while it is still active in the hub. -->
        <receiver
            android:name="com.snowmedia.notify.AlertDismissReceiver"
            android:exported="false" />

        <!-- Home-screen news widget. Only launchers that expose a widget picker
             will ever bind this (generic Android boxes); the stock Android TV /
             Google TV and Fire TV launchers ignore it entirely. -->
        <receiver
            android:name="com.snowmedia.widget.SmcNewsWidget"
            android:exported="false">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/smc_news_widget_info" />
        </receiver>

    </application>

</manifest>
